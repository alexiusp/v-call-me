/// Template for `turn_config.dart`, which is gitignored because it holds a
/// real API key. Copy this file to `turn_config.dart` in the same directory
/// and fill in your own Metered ("Open Relay Project") API key from
/// https://www.metered.ca/tools/openrelay/ - the dashboard shows the app
/// name and key used in:
/// `https://<app-name>.metered.live/api/v1/turn/credentials?apiKey=<key>`
const meteredApiKey = '';
